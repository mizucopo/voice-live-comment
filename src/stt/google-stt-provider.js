import { SttProvider } from './stt-provider.js';

export class GoogleSttProvider extends SttProvider {
  constructor(apiKey, language) {
    super();
    throw new Error('Googleプロバイダーはまだ実装されていません');
  }
}
